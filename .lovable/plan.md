
User wants to remove the "Copy" and "Regenerate" buttons (the two icons shown in screenshot) from the Insights card header.

Plan: In `src/components/intelligence/MarketIntelligenceInsightsCard.tsx`, remove the Copy and Regenerate buttons (and their tooltips/handlers/imports). Keep only the title and the cached timestamp badge.
