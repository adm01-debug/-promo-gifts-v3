-- LGPD Compliance: Automatic Purge Script
-- Purpose: Anonymize or delete lead data older than 90 days if inactive.

-- Placeholder for real implementation logic
-- DELETE FROM quote_approval_tokens WHERE created_at < NOW() - INTERVAL '90 days';
